{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "scripts": {
        "build": "echo \"Frontend is Flutter; no Vite build.\"",
        "dev": "echo \"Use Flutter in flutter_app/\""
    }
}
